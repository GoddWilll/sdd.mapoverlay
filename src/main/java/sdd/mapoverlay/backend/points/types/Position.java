/**
 * The Position enum represents the relative position of two points on a plane.
 * It can have three possible values: LEFT, RIGHT, or COLLINEAR.
 */
package sdd.mapoverlay.backend.points.types;

public enum Position {
    LEFT,
    RIGHT,
    COLLINEAR
}
